<div class="post-comment">
  <%= comment.user.name %>
  <%= comment.content %>
  <span class="moderate-font"><%= " (#{time_ago_in_words(comment.created_at)}前)" %></span>
    <u style="font-size:10px; font-style:oblique;"><%= link_to "削除", admins_comment_path(comment.id), method: :delete, data:{confirm: "本当に削除しますか？ #{comment.content}"} %></u>
</div>